GM AGENCY - FINAL SPREADSHEET DEPLOYMENT

1. GitHub repository MUST have:
   api/index.ts
   api/server.ts

2. DO NOT have:
   api/index.js
   api/server.js
   api/api-server-source.ts
   api-server-source.ts

3. Root server.ts may remain. It is for local development and is NOT the Vercel API entrypoint.

4. Vercel Environment Variables:
   GOOGLE_SHEETS_WEBHOOK_URL = Apps Script Web App URL ending in /exec
   SHEETS_WEBHOOK_SECRET = exactly the same value as SHARED_SECRET in Code.gs

5. Apps Script Code.gs:
   SPREADSHEET_ID = your spreadsheet ID
   SHARED_SECRET = same value as Vercel SHEETS_WEBHOOK_SECRET
   BACKEND_WEBHOOK_URL = https://www.gmsolution.store/api/sheets-webhook

6. Apps Script deployment:
   Type: Web app
   Execute as: Me
   Who has access: Anyone
   Use /exec URL, not /dev.

7. Apps Script trigger:
   Function: onEditInstallable
   Event source: From spreadsheet
   Event type: On edit

8. Sheet names must be exactly:
   Web_Orders
   Shopee_Orders
   Review_Orders

9. After deployment test:
   https://www.gmsolution.store/api/orders
   https://www.gmsolution.store/api/shopee_orders
   https://www.gmsolution.store/api/maps_reviews

10. Expected first success: JSON response (possibly []), NOT FUNCTION_INVOCATION_FAILED.
