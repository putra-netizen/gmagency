GM AGENCY - DEEP FIXED SPREADSHEET DEPLOYMENT

FINAL VERCEL API STRUCTURE
- api/[...path].ts is the ONLY Vercel Function entry under /api.
- Root server.ts is the Express application used by the Vercel catch-all.
- Do NOT create api/index.js, api/index.ts, api/server.ts, or api/server.js.

WHY
- /api/index.ts only maps to /api, not /api/orders.
- The catch-all function maps all /api/* requests to the same Express app.
- The SPA rewrite explicitly excludes /api so API requests are not rewritten to index.html.

EXPECTED REPO
api/
  [...path].ts
server.ts
src/
public/
package.json
package-lock.json
vercel.json
...

VERCEL ENV
GOOGLE_SHEETS_WEBHOOK_URL=<Apps Script Web App URL ending in /exec>
SHEETS_WEBHOOK_SECRET=<same value as SHARED_SECRET in Code.gs>

APPS SCRIPT
SPREADSHEET_ID=<your spreadsheet id>
SHARED_SECRET=<same as Vercel SHEETS_WEBHOOK_SECRET>
BACKEND_WEBHOOK_URL=https://www.gmsolution.store/api/sheets-webhook

Deploy Apps Script as:
- Type: Web app
- Execute as: Me
- Who has access: Anyone
- Use /exec URL, not /dev

TRIGGER
Function: onEditInstallable
Event source: From spreadsheet
Event type: On edit

SHEETS
Web_Orders
Shopee_Orders
Review_Orders

TEST
https://www.gmsolution.store/api/orders
https://www.gmsolution.store/api/shopee_orders
https://www.gmsolution.store/api/maps_reviews

Expected: JSON response. Empty data is OK: [].
