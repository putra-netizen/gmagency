GM AGENCY — SPREADSHEET / VERCEL CLEAN FIX

Final API architecture:
Browser -> /api/* -> api/[...path].ts -> root server.ts -> sheetsBridge.ts -> Google Apps Script -> Google Spreadsheet.

api/ must contain ONLY:
  [...path].ts

Do NOT add:
  api/server.ts
  api/index.ts
  api/index.js
  api/server.js

Root server.ts is the Express application and must remain.

build:vercel = vite build

Vercel environment variables:
  GOOGLE_SHEETS_WEBHOOK_URL
  SHEETS_WEBHOOK_SECRET

After deploy test:
  /api/health
  /api/orders
  /api/shopee_orders
  /api/maps_reviews
